# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.DS_Store
dist
dist-ssr
coverage
*.local

/cypress/videos/
/cypress/screenshots/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*.tsbuildinfo
.env.production
# Ignore local dev notes
.env.notes
chat-deployment-notes.txt

# Ignore compiled server files, since the server is run separately from the vite app and doesn't need to be tracked in git. 
# This also prevents accidentally committing secrets if they are used in the server code.
*src/chat/server.js
*src/types/index.js